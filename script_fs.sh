sudo apt update #This command gets all the updates for the system
sudo apt upgrade #This command upgrades the system with the update from before
sudo apt install parted #This command installs the app "parted"
