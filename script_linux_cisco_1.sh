ls #This command lists the files and directories in the current location
ls LICENSE #This command checks if a file named "LICENSE" exists in the current directory
aptitude moo #This command is an Easter egg that asks if there is a cow in the program
ls -l -r #This command lists files in long format and in reverse order
ls -rl #This command lists files in reverse order and long format
ls -lr #This command lists files in long format and reverse order
aptitude -v -v moo #This command gives a more verbose response to the cow Easter egg
aptitude -vv moo #This command gives a more verbose response to the cow Easter egg
pwd #This command shows the path of the current working directory
cd / #This command changes the current directory to the system root
cd .. #This command moves the current directory one level up to the parent directory
cd ~ #This command changes the current directory to the user's home folder
su - #This command switches the current user to the root user with its environment variables
exit #This command closes the current terminal session or logs out the current user
sudo sl #This command runs a "Steam Locomotive" animation with superuser privileges
ls -l hello.sh #This command shows the detailed information and permissions of the file "hello.sh"
./hello.sh #This command executes the script named "hello.sh" in the current directory
chmod u+x hello.sh #This command adds execution permissions for the owner of the file "hello.sh"
ls -l hello.sh #This command shows the updated permissions of "hello.sh" to verify changes
sudo chown root hello.sh #This command changes the ownership of the file "hello.sh" to the root user
ls -l hello.sh #This command verifies the change of ownership for the file "hello.sh"
./hello.sh #This command attempts to execute "hello.sh" with its current owner and permissions
cat animals.txt #This command displays the entire content of the file "animals.txt"
cat alpha.txt #This command displays the entire content of the file "alpha.txt"
head alpha.txt #This command shows the first ten lines of the file "alpha.txt"
tail alpha.txt #This command shows the last ten lines of the file "alpha.txt"
head -n 5 alpha.txt #This command shows exactly the first five lines of the file "alpha.txt"
tail -n 5 alpha.txt #This command shows exactly the last five lines of the file "alpha.txt"
cd ~/Documents #This command changes the current directory to the "Documents" folder
cp /etc/passwd . #This command copies the system password file to the current directory
cd ~ #This command returns the user to the home directory
dd if=/dev/zero of=/tmp/swapex bs=1M count=50 #This command creates a 50MB file named "swapex" filled with zeros in the /tmp directory